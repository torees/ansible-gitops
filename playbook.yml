---


-  name: Run deploy
   hosts: "{{app_servers}}"
   become: yes
   roles:
      - wildfly
   tasks:
      - file:
          path: /tmp/hello
          state: touch
      - command: " cat /etc/hosts"